import { Typography } from '@mui/material';

const ModalConfirmOrder = () => {
	return (
		<div>
			<Typography>Confirm Order</Typography>

			<Typography>ETH-PERP</Typography>

			<Typography>Limit Buy</Typography>
		</div>
	);
};

export default ModalConfirmOrder;
